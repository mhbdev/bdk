// Postgres migration moved to drizzle-kit.
// Run `pnpm drizzle-kit push` (script: `db:migrate`) to apply schema from drizzle.config.ts.
console.log('Migration script updated: use `pnpm run db:migrate` to push Postgres schema via drizzle-kit.');